/*const textos = ['Imagem 1', 'Imagem 2', 'Imagem 3']

const imagens = ['img1.png', 'img2.png', 'img3.png']

const divImagem = document.getElementById('imagem')

function selecionaImagem() {
  const opcao = Math.floor(Math.random() * imagens.length)
  divImagem.style.backgroundImage = url('imagens/${imagens[opcao]}')
}

setInterval(selecionaImagem, 3000)

const paragrafo = document.getElementById('texto')

let indice = 0

function trocaTexto() {
  paragrafo.innerText = textos[indice]
  indice++
  if (indice === textos.length) {
    indice = 0
  }
}

setInterval(trocaTexto, 2000)

function trocarImagem() {
  var imagem = document.getElementById('imagem')
  if (imagem.src.match('img1.png')) {
    imagem.src = 'img2.png'
  } else {
    imagem.src = 'img1.png'
  }
}*/
